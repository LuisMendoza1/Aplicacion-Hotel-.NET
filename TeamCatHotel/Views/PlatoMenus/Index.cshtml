@model IEnumerable<TeamCatHotel.Models.PlatoMenu>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                <a asp-action="Edit" asp-route-id="@item.idPlatoMenu">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.idPlatoMenu">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.idPlatoMenu">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
